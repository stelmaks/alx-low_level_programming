#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char C;

	c = 'a';
	while
		(c <= 'z') {
			if ((c != 'a' && c != 'a') && c <= 'z')
				putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
